---
import { ContentImage, HeroImage, FixedImage } from './images';
import heroImage from '../assets/images/breautyflow-fokep.jpg';
import googleLogo from '../assets/images/google-logo.jpg';
import notinoIcon from '../assets/images/notino-ikon.png';
import { type Locale, defaultLang, getLocalizedPath } from '../i18n';

interface Props {
  lang?: Locale;
}

const { lang = defaultLang } = Astro.props;

const content = {
  hu: {
    stats: [
      { value: '10+ év', label: 'tapasztalat' },
      { value: '10 000+', label: 'kezelés' },
      { value: '12', label: 'csúcstechnológiájú gép' },
      { value: '1500+', label: 'elégedett vendég' },
    ],
    googleRating: 'Google értékelés alapján',
    googleRatingMobile: 'Google értékelés',
    notinoBadge: 'Notino "Hónap Szalonja"',
    subtitle: 'Szépségszalon és lézeres szőrtelenítés',
    title: 'A legcsajosabb szépségszalon',
    location: 'budán és pesten',
    description: 'Kapj szakszerű kezelést és teljes, osztatlan figyelmet<br />a díjnyertes budai és pesti központunkban.',
    cta: 'Kérj ingyenes konzultációt',
    awards: {
      entrepreneur: 'Év Vállalkozója-díj',
      notinoPartner: 'Notino Partner',
      fairBusiness: 'Tisztességes Vállalkozás',
      turulGold: 'Turul Szépség Gold',
    },
    imageAlt: 'Beautyflow szépségszalon',
  },
  en: {
    stats: [
      { value: '10+ yrs', label: 'experience' },
      { value: '10,000+', label: 'treatments' },
      { value: '12', label: 'modern machines' },
      { value: '1500+', label: 'happy clients' },
    ],
    googleRating: 'Based on Google reviews',
    googleRatingMobile: 'Google rating',
    notinoBadge: 'Notino "Salon of the Month"',
    subtitle: 'Beauty salon and laser hair removal',
    title: 'The coolest beauty salon',
    location: 'in Buda and Pest',
    description: 'Get professional treatments and full, undivided attention<br />at our award-winning Buda and Pest locations.',
    cta: 'Request Free Consultation',
    awards: {
      entrepreneur: 'Entrepreneur of the Year',
      notinoPartner: 'Notino Partner',
      fairBusiness: 'Fair Business Award',
      turulGold: 'Turul Beauty Gold',
    },
    imageAlt: 'Beautyflow beauty salon',
  },
};

const t = content[lang];
const consultationPath = getLocalizedPath(lang === 'hu' ? '/ingyenes-konzultacio' : '/free-consultation', lang);
---

<section class="relative flex flex-col lg:-mt-[60px] lg:h-[100dvh]">
  <div class="flex flex-col lg:flex-row lg:flex-1 lg:min-h-0">
    <!-- Left side - Image with Google rating badge -->
    <div class="hidden lg:flex lg:w-1/2 relative">
      <ContentImage
        src={heroImage}
        alt={t.imageAlt}
        class="absolute inset-0 w-full h-full object-cover"
        priority={true}
      />
      <!-- Google Rating Badge - bottom right corner -->
      <div class="absolute bottom-6 right-6 bg-white/95 backdrop-blur-sm rounded-xl shadow-xl p-4 google-rating-badge">
        <div class="flex items-center gap-3">
          <FixedImage src={googleLogo} alt="Google" width={40} class="rounded-full object-cover" />
          <div>
            <div class="flex items-center gap-1 mb-1">
              <span class="text-2xl font-bold text-gray-900 rating-number" data-target="5.0">0</span>
              <div class="flex">
                {[1, 2, 3, 4, 5].map(() => (
                  <svg class="w-4 h-4 text-orange-400" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                  </svg>
                ))}
              </div>
            </div>
            <p class="text-xs text-gray-600">{t.googleRating}</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Right side - Content -->
    <div class="lg:w-1/2 bg-[#fff9f7] flex items-center justify-center pt-0 pb-8 lg:pt-[80px] lg:pb-6">
      <div class="w-full px-5 text-center">
        <!-- Mobile image with Google rating -->
        <div class="relative lg:hidden -mx-5 mb-6">
          <HeroImage
            src={heroImage}
            alt={t.imageAlt}
            class="w-full h-48 object-cover aspect-[16/9]"
            priority={true}
          />
          <!-- Mobile Google Rating Badge -->
          <div class="absolute bottom-3 right-3 bg-white/95 backdrop-blur-sm rounded-lg shadow-lg p-2 google-rating-badge">
            <div class="flex items-center gap-2">
              <FixedImage src={googleLogo} alt="Google" width={24} class="rounded-full object-cover" />
              <div>
                <div class="flex items-center gap-1">
                  <span class="text-lg font-bold text-gray-900 rating-number" data-target="5.0">0</span>
                  <svg class="w-3 h-3 text-orange-400" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                  </svg>
                </div>
                <p class="text-[10px] text-gray-600">{t.googleRatingMobile}</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Notino Badge - ABOVE H1 -->
        <div class="inline-flex items-center gap-2 mb-5">
          <div class="relative">
            <div class="absolute inset-0 bg-gradient-to-br from-yellow-400 to-yellow-600 rounded-full blur-sm opacity-50"></div>
            <div class="relative bg-gradient-to-br from-yellow-300 via-yellow-400 to-yellow-500 p-1 rounded-full">
              <div class="bg-white rounded-full p-1">
                <FixedImage src={notinoIcon} alt="Notino" width={32} class="w-8 h-8" />
              </div>
            </div>
          </div>
          <div class="text-left">
            <div class="flex items-center gap-1">
              <svg class="w-3 h-3 text-yellow-500" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" /></svg>
              <span class="text-xs font-semibold text-gray-700 uppercase tracking-wide">{lang === 'hu' ? 'Díjnyertes szalon' : 'Award-winning salon'}</span>
            </div>
            <span class="text-sm font-bold text-[#c53f75]">{t.notinoBadge}</span>
          </div>
        </div>

        <h1 class="text-xs uppercase tracking-widest text-gray-500 mb-3 font-sans font-normal">
          {t.subtitle}
        </h1>

        <h2 class="font-heading text-[1.4rem] sm:text-2xl md:text-3xl lg:text-[2.4rem] text-gray-900 mb-1 leading-tight uppercase font-bold">
          {t.title}
        </h2>
        <p class="font-heading text-lg sm:text-xl md:text-2xl lg:text-3xl text-gray-900 mb-4 uppercase font-bold">
          {t.location}
        </p>

        <p class="text-gray-600 mb-4 text-sm" set:html={t.description} />

        <!-- CTA: Button + Phone below -->
        <div class="flex flex-col items-center justify-center gap-2 mb-4">
          <a href={consultationPath} class="inline-block px-5 py-3 bg-[#c53f75] text-white font-bold uppercase text-sm tracking-wide rounded-full shadow-lg hover:bg-[#a33460] hover:shadow-xl transition-all transform hover:scale-105">
            {t.cta}
          </a>
          <a href="tel:+3613009414" class="inline-flex items-center gap-2 text-base lg:text-lg text-[#c53f75] font-bold hover:text-[#a33460] transition-all duration-300">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
            </svg>
            +36 1 300 9414
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Stats Row - positioned at the bottom of the hero, always above the fold -->
  <div class="w-full bg-gradient-to-r from-[#c53f75] to-[#a33460] py-4 lg:h-[80px] flex-shrink-0 flex items-center justify-center">
    <div class="max-w-[1000px] mx-auto px-4 w-full">
      <div class="grid grid-cols-2 lg:grid-cols-4 gap-3 lg:gap-8 h-full items-center">
        {t.stats.map((stat) => (
          <div class="text-center">
            <p class="text-[24px] lg:text-[32px] font-heading text-white font-bold leading-[0.9] mb-1">
              {stat.value}
            </p>
            <p class="text-white/80 text-[12px] lg:text-[16px] leading-[0.9]">{stat.label}</p>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Trust Badges / Awards Row - Elegant Design -->
  <div class="w-full bg-[#1a1a1a] py-5">
    <div class="max-w-[1100px] mx-auto px-4">
      <!-- Mobile: 2x2 grid, Desktop: flex row -->
      <div class="grid grid-cols-2 gap-4 sm:flex sm:flex-wrap sm:justify-center sm:items-center sm:gap-6 lg:gap-12">
        <!-- Év Vállalkozója díj -->
        <div class="flex items-center gap-2 sm:gap-3">
          <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-gradient-to-br from-amber-400 to-amber-600 flex items-center justify-center flex-shrink-0">
            <svg class="w-3.5 h-3.5 sm:w-4 sm:h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
              <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
            </svg>
          </div>
          <span class="text-xs sm:text-sm font-medium text-white/90 tracking-wide">{t.awards.entrepreneur}</span>
        </div>

        <div class="hidden sm:block w-px h-6 bg-white/20"></div>

        <!-- Notino Partner -->
        <div class="flex items-center gap-2 sm:gap-3">
          <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-white flex items-center justify-center flex-shrink-0">
            <FixedImage src={notinoIcon} alt="Notino" width={20} class="w-4 h-4 sm:w-5 sm:h-5" />
          </div>
          <span class="text-xs sm:text-sm font-medium text-white/90 tracking-wide">{t.awards.notinoPartner}</span>
        </div>

        <div class="hidden sm:block w-px h-6 bg-white/20"></div>

        <!-- Tisztességes Vállalkozás -->
        <div class="flex items-center gap-2 sm:gap-3">
          <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-gradient-to-br from-emerald-400 to-emerald-600 flex items-center justify-center flex-shrink-0">
            <svg class="w-3.5 h-3.5 sm:w-4 sm:h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M5 13l4 4L19 7" />
            </svg>
          </div>
          <span class="text-xs sm:text-sm font-medium text-white/90 tracking-wide">{t.awards.fairBusiness}</span>
        </div>

        <div class="hidden sm:block w-px h-6 bg-white/20"></div>

        <!-- Turul Szépség Gold -->
        <div class="flex items-center gap-2 sm:gap-3">
          <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-gradient-to-br from-amber-300 to-yellow-500 flex items-center justify-center flex-shrink-0">
            <span class="text-[10px] sm:text-xs font-bold text-amber-900">G</span>
          </div>
          <span class="text-xs sm:text-sm font-medium text-white/90 tracking-wide">{t.awards.turulGold}</span>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  function initRatingAnimation() {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    const ratingElements = document.querySelectorAll('.rating-number');

    if (prefersReducedMotion) {
      ratingElements.forEach(el => {
        el.textContent = el.getAttribute('data-target') || '5.0';
      });
      return;
    }

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const el = entry.target as HTMLElement;
          const target = parseFloat(el.getAttribute('data-target') || '5.0');
          animateRating(el, target);
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });

    ratingElements.forEach(el => observer.observe(el));
  }

  function animateRating(element: HTMLElement, target: number) {
    const duration = 1500;
    const start = 1;
    const startTime = performance.now();

    function update(currentTime: number) {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);

      // Easing function for smooth animation
      const easeOut = 1 - Math.pow(1 - progress, 3);
      const current = start + (target - start) * easeOut;

      element.textContent = current.toFixed(1);

      if (progress < 1) {
        requestAnimationFrame(update);
      }
    }

    requestAnimationFrame(update);
  }

  document.addEventListener('DOMContentLoaded', initRatingAnimation);
  document.addEventListener('astro:page-load', initRatingAnimation);
</script>

<style>
  .google-rating-badge {
    animation: fadeInUp 0.8s ease-out 0.5s forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .google-rating-badge {
      animation: none;
      opacity: 1;
    }
  }
</style>
