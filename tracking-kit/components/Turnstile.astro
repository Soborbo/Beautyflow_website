---
/**
 * Turnstile `api.js` betöltő — AZ ŰRLAPOKNAK, nem a trackingnek.
 *
 * ── Mi változott (2026-08-28) ────────────────────────────────────────────────
 * Ez a komponens eredetileg a tracking-dispatch LÁTHATATLAN widgetjét hozta
 * (`#cf-turnstile-invisible`) + egy prewarm-ot. A gateway viszont 2026 nyara
 * óta NEM validál Turnstile-t, tehát a token semmit nem ért — cserébe a
 * `sendToWorker` minden dispatch előtt megvárta a widgetet (10 mp-es
 * timeouttal), a klikk kritikus útján. A tracking-rész ezért kikerült.
 *
 * ── Miért MARAD mégis ez a komponens ─────────────────────────────────────────
 * Az `api.js`-t EZ tölti be, oldal-globálisan, és erre épülnek az ŰRLAPOK saját,
 * LÁTHATÓ widgetjei (`src/lib/forms/turnstile-client.ts` → /api/contact,
 * /api/boranalizis). Több oldal kommentje is kimondja, hogy „az api.js-t egyszer,
 * page-globálisan a Layout <Turnstile/>-ja tölti, ezért itt nincs saját api.js".
 * Ha ezt a fájlt törölnénk, az űrlap-spamvédelem némán elszállna.
 *
 * A `?render=explicit` KÖTELEZŐ: enélkül az api.js DOM-ot szkennel `.cf-turnstile`
 * osztályra, és az auto-render ütközik az űrlapok explicit renderelésével.
 */
---

<script is:inline src="https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit" async defer></script>
