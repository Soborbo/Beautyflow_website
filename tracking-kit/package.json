{
  "name": "soborbo-tracking",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical Astro lead-gen tracking — browser (GTM) + server (event-gateway worker)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check:events": "node server/check-event-contract.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
